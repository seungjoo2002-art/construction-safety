# 건설사고 예측 모델 — 서빙용 (위험도 / 사고유형)
# ============================================================
# 학습 환경: Windows, conda, Python 3.12
# ※ 버전을 고정합니다. 다른 버전에서는 joblib 로딩이 실패하거나
#   예측값이 미묘하게 달라질 수 있습니다.
# ============================================================
numpy==2.2.2
pandas==2.2.3
scikit-learn==1.6.1
xgboost==3.0.0
lightgbm==4.6.0
catboost==1.2.10
joblib==1.4.2

# ============================================================
# app.py 서버 실행 + similarity_service.py(유사도 분석) 구동에 필요한 패키지.
# 버전 고정 없음 — 최신 버전으로 설치해도 무방합니다.
# ============================================================
fastapi
uvicorn[standard]
pydantic
requests
openai
scipy
matplotlib
seaborn
openpyxl

# ============================================================
# 사진 분석(/api/analyze-photo, safety_yolo_pkg) 구동에 필요한 패키지.
# GPU 없어도 동작하지만 CPU는 한 장에 2~5초 걸릴 수 있습니다.
# ============================================================
ultralytics>=8.3
torch
torchvision
pillow

# ============================================================
# (선택) ADVISOR_LLM=exaone — 해결방안 생성을 로컬 EXAONE-4.0-1.2B로 돌릴 때만 필요.
# Render 배포(기본값 ADVISOR_LLM=gemini)에서는 설치하지 않아도 됩니다.
# 모델은 첫 실행 때 Hugging Face에서 자동으로 받습니다(약 2.6GB, 토큰 불필요).
# ※ dtype= 인자를 쓰므로 transformers 4.56 이상이 필요합니다.
# ============================================================
# transformers>=4.56

# 설치
#   pip install -r requirements.txt
#
# joblib.load()에서 오류가 나면 conda로 설치해 보세요
#   conda install -c conda-forge scikit-learn=1.6.1 xgboost=3.0.0 lightgbm=4.6.0 catboost=1.2.10
